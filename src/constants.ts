// Central export for portfolio data. Point directly to the JavaScript source to avoid circular imports.
// Re-export portfolio data. Using the module name without the .js extension works with TypeScript's module resolution.
// Re-export portfolio data from the JavaScript module.
export { portfolioData } from './data.js';
